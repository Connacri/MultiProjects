/// @deprecated Use [ShiftType] as the single canonical domain shift enum.
///
/// Kept temporarily as a source-compatible alias target during migration.
/// New domain code must import `shift_type.dart` directly.
import 'shift_type.dart';

/// Legacy compatibility typedef. Do not use for new code.
typedef TeamShift = ShiftType;
